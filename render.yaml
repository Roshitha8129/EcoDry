services:
  - type: web
    name: ecodry-app
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 1 wsgi:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
